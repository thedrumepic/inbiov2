# 🚀 Инструкция по деплою Inbio.one

## 📋 Требования к серверу

### Ваш Hetzner VPS:
- **CPU**: 2 vCPU ✅
- **RAM**: 4GB ✅
- **Storage**: 40GB local disk ✅
- **OS**: Ubuntu 20.04+ / Debian 11+

### Программное обеспечение:
- Docker & Docker Compose
- Git
- Node.js 18+ (для сборки frontend)
- MongoDB
- Текстовый редактор (nano/vim)

## 🔧 Подготовка к деплою

### 1. Подключение к серверу
```bash
# SSH подключение к Hetzner VPS
ssh root@your-server-ip

# Создаем пользователя для приложения (рекомендуется)
adduser inbio
usermod -aG sudo inbio
su - inbio
```

### 2. Установка Docker
```bash
# Обновляем систему
sudo apt update && sudo apt upgrade -y

# Устанавливаем зависимости
sudo apt install -y curl git wget nano

# Устанавливаем Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER

# Устанавливаем Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Проверяем установку
docker --version
docker-compose --version
```

### 3. Установка Node.js
```bash
# Устанавливаем Node.js 18
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверяем установку
node --version
npm --version
```

### 4. Установка MongoDB
```bash
# Импортируем ключ MongoDB
wget -qO - https://www.mongodb.org/static/pgp/server-7.0.asc | sudo apt-key add -

# Добавляем репозиторий
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu focal/mongodb-org/7.0 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-7.0.list

# Устанавливаем MongoDB
sudo apt-get update
sudo apt-get install -y mongodb-org

# Запускаем и добавляем в автозагрузку
sudo systemctl start mongod
sudo systemctl enable mongod

# Проверяем статус
sudo systemctl status mongod
```

### 5. Клонирование репозитория
```bash
# Клонируем проект
git clone <your-repo-url> inbio.one
cd inbio.one

# Проверяем структуру
ls -la
```

### 6. Настройка MongoDB
```bash
# Создаем пользователя для MongoDB
mongo

# В MongoDB shell
use admin
db.createUser({
  user: "admin",
  pwd: "admin123456",
  roles: ["userAdminAnyDatabase", "dbAdminAnyDatabase", "readWriteAnyDatabase"]
})

exit
```

### 7. Настройка переменных окружения
```bash
# Копируем production шаблон (уже готовые переменные!)
cp .env.production .env

# Проверяем переменные
cat .env
```

**Готовые переменные в .env:**
- `MONGO_PASSWORD`: admin123456
- `JWT_SECRET_KEY`: super-secret-key-inbio-2026
- `OWNER_EMAIL`: thedrumepic@gmail.com
- `BASE_URL`: https://inbio.one
- `RESEND_API_KEY`: готовый ключ
- `Telegram токены`: готовые токены

**Примечание:** Google авторизация отключена, работает только email/пароль регистрация.

### 8. Настройка домена и SSL
```bash
# Убедитесь что домен inbio.one указывает на IP сервера
nslookup inbio.one

# Настраиваем firewall (только 80, 443 порты)
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
```

## 🐳 Деплой через Docker Compose

### 1. Сборка и запуск
```bash
# Останавливаем старые контейнеры (если есть)
docker-compose down

# Собираем и запускаем
docker-compose up -d --build

# Проверяем статус
docker-compose ps
```

### 2. Установка зависимостей Frontend
```bash
# Заходим в папку frontend
cd frontend

# Устанавливаем зависимости (важно - node_modules удален!)
npm install

# Возвращаемся в корень
cd ..
```

### 3. Проверка работоспособности
```bash
# Проверяем логи
docker-compose logs -f backend
docker-compose logs -f frontend

# Проверяем API
curl http://localhost:8000/api/health

# Проверяем фронтенд
curl http://localhost:3000

# Проверяем MongoDB
mongo --eval "db.adminCommand('ismaster')"
```

### 4. Настройка HTTPS
Caddy автоматически получит SSL сертификат от Let's Encrypt. Убедитесь, что:
- Домен указывает на сервер
- Порт 80 и 443 открыты
- Файрвол не блокирует соединения

## 📁 Структура после деплоя

```
inbio.one/
├── .env                    # Переменные окружения
├── docker-compose.yml     # Docker конфигурация
├── Caddyfile              # Веб-сервер конфиг
├── backend/               # Python FastAPI
├── frontend/              # React сборка
└── uploads/               # Загруженные файлы
```

## 🔍 Мониторинг и отладка

### Проверка логов:
```bash
# Все сервисы
docker-compose logs

# Конкретный сервис
docker-compose logs backend
docker-compose logs frontend
docker-compose logs mongodb
docker-compose logs caddy
```

### Перезапуск сервисов:
```bash
# Перезапустить все
docker-compose restart

# Конкретный сервис
docker-compose restart backend
```

### Обновление приложения:
```bash
# Получаем изменения
git pull

# Пересобираем и перезапускаем
docker-compose up -d --build
```

## ⚠️ Возможные проблемы (Hetzner VPS)

### 1. Проблема: Недостаточно памяти для сборки
**Решение:**
```bash
# Создаем swap файл (добавит 2GB памяти)
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# Проверяем
free -h
```

### 2. Проблема: MongoDB не запускается
**Решение:**
```bash
# Проверяем статус MongoDB
sudo systemctl status mongod

# Проверяем логи
sudo tail -f /var/log/mongodb/mongod.log

# Перезапускаем
sudo systemctl restart mongod
```

### 3. Проблема: Frontend не собирается
**Решение:**
```bash
# Очищаем кэш npm
cd frontend
rm -rf node_modules package-lock.json
npm cache clean --force
npm install

# Собираем локально для проверки
npm run build

# Если ошибка памяти, увеличиваем swap
```

### 4. Проблема: SSL сертификат не получен
**Решение:**
```bash
# Проверяем домен
nslookup inbio.one

# Проверяем порты
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :443

# Проверяем логи Caddy
docker-compose logs caddy

# Пересоздаем сертификат
docker-compose exec caddy caddy reload
```

### 5. Проблема: Несколько проектов на одном VPS
**Решение:**
```bash
# Используем разные порты для других проектов
# Inbio.one использует: 80, 443 (через Caddy)
# Backend: 8000 (внутри Docker)
# Frontend: 3000 (внутри Docker)

# Проверяем занятые порты
sudo netstat -tlnp
```

### 6. Проблема: API не отвечает
**Решение:**
```bash
# Проверяем переменные окружения
docker-compose exec backend env | grep MONGO

# Проверяем подключение к MongoDB
docker-compose exec backend python -c "from mock_db import AsyncMockClient; print('DB OK')"

# Проверяем логи backend
docker-compose logs backend
```

## 📊 Производительность (Hetzner VPS)

### Оптимизация для 2GB RAM:
1. **Swap файл**: Создан 2GB swap для сборки
2. **MongoDB**: Используйте реальную MongoDB с ограничениями памяти
3. **Фронтенд**: Сжатие gzip в Caddyfile
4. **Изображения**: Автооптимизация при загрузке
5. **Docker**: Лимиты памяти для контейнеров


## 🔄 Резервное копирование

### База данных MongoDB:
```bash
# Создаем бэкап
mongodump --db inbio --out /backup/$(date +%Y%m%d)

# Восстановление
mongorestore --db inbio /backup/20240223/inbio

# Автоматический бэкап (добавить в crontab)
0 2 * * * /usr/bin/mongodump --db inbio --out /backup/$(date +\%Y\%m\%d)
```

### Файлы:
```bash
# Бэкап uploaded файлов
tar -czf uploads-backup-$(date +%Y%m%d).tar.gz backend/uploads/

# Восстановление
tar -xzf uploads-backup-20240223.tar.gz
```

## � Аутентификация

**Важно:** В этом приложении отключена Google OAuth авторизация. Работает только:
- **Email/пароль регистрация**
- **Восстановление пароля через email**

**Сессии пользователей:**
- JWT токены с сроком действия 7 дней
- Надежное хранение в localStorage
- Автоматическое обновление при необходимости

**Для стабильности сессий:**
1. Используйте сильный `JWT_SECRET_KEY`
2. Убедитесь что CORS настроен правильно
3. Проверьте что домен в `BASE_URL` совпадает с реальным

## ��️ Безопасность

### Обязательно настройьте:
1. **Сложные пароли** в .env
2. **Файрвол** - откройте только 80, 443 порты
3. **Регулярные обновления** Docker и системы
4. **Бэкапы** базы данных и файлов
5. **Мониторинг** логов на подозрительную активность

### Рекомендации:
- Используйте отдельного пользователя для приложения
- Настройте логротацию
- Ограничьте права доступа к директориям
- Используйте секреты Docker вместо .env в продакшн

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи всех сервисов
2. Убедитесь, что все переменные окружения настроены
3. Проверьте сетевые настройки и файрвол
4. Очистите Docker кэш: `docker system prune -a`

---

**Готово!** После выполнения этих шагов ваше приложение Inbio.one будет работать в продакшн режиме с автоматическим HTTPS и высокой доступностью. 🎉
